apiVersion: v1
kind: Pod
metadata:
  name: elasticsearch
  labels:
    app: elastcisearch
spec:
  containers:
    - name: elasticsearch
      image: selesabraham/elasticsearch:latest
      command: ["printenv"]
      args: ["seles2112", "8080"]
  restartPolicy: OnFailure
      