kind: Service
apiVersion: v1
metadata:
  name: elasticsearch
spec:
  selector:
    app: elasticsearch
  ports:
  - nodePort: 32444
    port: 8080
    protocol: TCP
    targetPort: 8080
  type: NodePort
  